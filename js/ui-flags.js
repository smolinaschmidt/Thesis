/**
 * Toggle sentiment analysis in the public UI (ch.04 pinned charts, related copy).
 * Morfología y datos siguen en el repo; pon a `true` para volver a mostrarlo.
 */
export const SHOW_SENTIMENT_UI = false;
